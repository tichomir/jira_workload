version: "3.9"

# ─────────────────────────────────────────────────────────────────────────────
# jira_workload — local Docker Compose stack
#
# Quick start:
#   macOS / Linux:  ./start.sh
#   Windows:        .\start.ps1  (or start.bat)
#
# Manual start:
#   cp config/deployment.env.example .env   # then fill in credentials
#   docker compose up --build
# ─────────────────────────────────────────────────────────────────────────────

services:
  app:
    build:
      context: .
    image: jira-workload:latest
    env_file:
      - .env
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      PORT:     ${PORT:-4000}
      # Persistent data directory — db.json (connections, OAuth tokens, backup
      # metadata) is read/written here.  The named volume db_data is mounted at
      # /data so upgrades never touch stored data.
      DATA_DIR: /data
    ports:
      - "${PORT:-4000}:${PORT:-4000}"
    volumes:
      - db_data:/data
      - backup_data:/app/data/backups
      - sdi_tmp:/app/data/sdi-tmp
      - export_data:/app/data/exports
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:${PORT:-4000}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped

# ─── Named volumes (persisted across container restarts) ──────────────────────
volumes:
  db_data:
    driver: local
  backup_data:
    driver: local
  sdi_tmp:
    driver: local
  export_data:
    driver: local
