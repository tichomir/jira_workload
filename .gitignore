
# PersonaForge sprint context (auto-generated, safe to delete)
.persona-snapshot.md

# Environment secrets — never commit real credentials
.env

# Local compose overrides — user-specific port/volume/service tweaks
# Copy podman-compose.override.yml.example to create this file.
podman-compose.override.yml

# Node.js dependencies
node_modules/

# Local TLS certificates (generated by mkcert for Caddy Option B — never commit private keys)
certs/
